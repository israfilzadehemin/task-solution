spring:
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:

  liquibase:
    enabled: true
    change-log: classpath:liquibase/changelog-master.yml
    drop-first: false
    database-change-log-table: DEFAULT_DBCHANGELOG
    database-change-log-lock-table: DEFAULT_DBCHANGELOGLOCK